# Python's private processing database (never passed to the API).
CHAMPIONGG_DB=data/champions.db
# Shared publication directory: Python writes; the API reads.
CHAMPIONGG_GOLD_DIR=data/gold
# Only the Python processor uses this credential.
RIOT_API_KEY=
# Docker Compose host port; API container always listens on 8000.
CHAMPIONGG_PORT=8000
